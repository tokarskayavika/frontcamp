import React from 'react';
import image from '../../style/images/unknown.jpg';

const Unknown = props => (
    <div className="unknown-page">
        <img src={image} />
    </div>
);

export default Unknown;